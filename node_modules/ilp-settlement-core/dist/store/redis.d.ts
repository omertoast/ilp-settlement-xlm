import Redis from 'ioredis';
import { SettlementStore } from '.';
export interface RedisOpts extends Redis.RedisOptions {
    client?: Redis.Redis;
    uri?: string;
}
export declare const connectRedis: ({ client, uri, ...opts }?: RedisOpts) => Promise<SettlementStore>;
